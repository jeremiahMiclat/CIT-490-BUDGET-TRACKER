import { Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function DailyLogsSpent() {
  return (
    <SafeAreaView>
      <View>
        <Text>Spent Screen</Text>
      </View>
    </SafeAreaView>
  );
}
