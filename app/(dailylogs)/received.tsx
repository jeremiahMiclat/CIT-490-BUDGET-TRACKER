import { Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function DailyLogsReceived() {
  return (
    <SafeAreaView>
      <View>
        <Text>Receive Screen</Text>
      </View>
    </SafeAreaView>
  );
}
